import {} from '../actionTypes';

const getInitialState = () => ({});

export const product = (state = getInitialState(), action) => {
  switch (action.type) {
    default:
      return state;
  }
};
