/* eslint-disable no-undef */
import {WIDTH} from '../helper/utils';

export default Fonts = {
  EXTRA_LARGE: WIDTH > 550 ? 22 : 20,
  LARGE: WIDTH > 550 ? 20 : 18,
  MEDIUM: WIDTH > 550 ? 18 : 16,
  SMALL: WIDTH > 550 ? 14 : 14,
};
